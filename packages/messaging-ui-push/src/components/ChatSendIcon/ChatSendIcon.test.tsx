import React from "react";
import { render } from "@testing-library/react";
import { ChatSendIcon } from "..";

describe("ChatSendIcon", () => {
  test("renders the ChatSendIcon component", () => {
    render(<ChatSendIcon onClick={() => {}} />);
  });
});
