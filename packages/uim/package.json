{
  "name": "@dapp-sdk/uim",
  "version": "1.0.0",
  "description": "User Identity Management Module by Meroku",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:clean": "rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@passwordless-id/webauthn": "^1.3.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "keywords": [
    "meroku",
    "uim-sdk"
  ],
  "devDependencies": {
    "@types/node": "^20.8.2"
  }
}
